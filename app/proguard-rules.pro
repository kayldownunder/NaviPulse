# Project-specific R8 / ProGuard rules.
# Room, Compose, coroutines, DataStore and Play Services ship their own consumer
# rules, so none are needed here. Add narrowly-scoped rules only for code that
# is accessed via reflection.
